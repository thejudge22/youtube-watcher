services:
  app:
    build: .
    ports:
      - "38000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/youtube-watcher.db
    user: "1000:1000"
